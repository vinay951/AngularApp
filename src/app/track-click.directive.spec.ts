import { TrackClickDirective } from './track-click.directive';

describe('TrackClickDirective', () => {
  it('should create an instance', () => {
    const directive = new TrackClickDirective();
    expect(directive).toBeTruthy();
  });
});
